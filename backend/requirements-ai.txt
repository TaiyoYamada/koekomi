# ===== AI バックエンド用の重い依存（GPU 推奨）=====
# TTS_BACKEND=qwen のとき必要。
# Colab では requirements.txt の後にこれを入れる（colab_runner が自動でやる）。
#
#   pip install -r backend/requirements-ai.txt
#
# ※ dummy で動作確認するだけなら不要。

# Colab には torch が同梱されているので通常そのまま使われる。
torch
numpy
soundfile

# 音声生成: Qwen3-TTS（声クローン対応・Apache-2.0）
#   https://github.com/QwenLM/Qwen3-TTS
qwen-tts

# 任意の高速化（ビルドに時間がかかる/失敗することがある。無くても sdpa で動く）
# flash-attn
