# 開発・テスト用（本番の requirements.txt とは分離）
-r requirements.txt

pytest==9.1.1
httpx==0.28.1          # FastAPI TestClient が内部で使う
ruff==0.15.19          # lint + format

# --- 静的検査 ---------------------------------------------------------------
mypy==2.3.1            # 型ヒントを飾りにしない
import-linter==2.13    # 層の依存契約を機械で守る（backend/.importlinter）

# --- テスト -----------------------------------------------------------------
hypothesis==6.165.9    # プロパティテスト（任意の入力で不変条件を確かめる）
schemathesis==4.24.3   # OpenAPI から生成した契約テスト
