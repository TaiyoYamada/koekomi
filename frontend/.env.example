# ===== フロントエンド設定（コピーして .env を作成）=====

# GAS（Google Apps Script）の Web アプリ URL。
# サーバーレジストリ（使えるColab一覧）を取得する先。
# 例: https://script.google.com/macros/s/XXXXXXXX/exec
VITE_GAS_URL=

# サーバー一覧の lastSeen が「新しい」と判定する許容秒数（任意）。
# これより古い heartbeat のサーバーは割り当て候補から外す。
VITE_SERVER_FRESH_SECONDS=120
