# セキュリティ

コエコミは**小学生の声**を扱います。声は個人情報・生体情報にあたり得るため、
「何を守っていて、何は守れていないか」をここに明記します。

## 脆弱性を見つけたら

公開の Issue ではなく、リポジトリの管理者に直接連絡してください。
イベント中に見つけた場合は、まず `/admin` からフォールバックモードに
切り替えて子どもの体験を止めないようにしてから連絡してください。

---

## 何を守っているか

### 1. 認証

`/health` を除くすべてのエンドポイントに `EVENT_TOKEN` が必要です
（`X-Event-Token` ヘッダー、または `?t=`）。

未設定でも起動はしますが、**起動ログで警告**します。

### 2. 保持期間（TTL）

すべての一時データに期限があり、掃除スレッドが確実に削除します。

| データ                     | 既定  | 変数               |
| -------------------------- | ----- | ------------------ |
| 参照音声（子どもの生の声） | 1時間 | `VOICE_TTL_SEC`    |
| 生成音声（AIの声）         | 1時間 | `ARTIFACT_TTL_SEC` |
| 動画                       | 30分  | `VIDEO_TTL_SEC`    |

- 参照音声は期限切れで**ファイルごと削除**されます。
- `DELETE /voices/{id}` で即時削除できます（録り直し時に自動で呼ばれます）。
- `POST /cleanup` で全部消せます。

### 3. 推測できない名前

生成物のIDは `uuid4`（128bit）で、順番に増えません。
URLを知らない人は引けません。

### 4. 入力の検証

- 生成物IDは正規表現（`^[0-9a-f]{32}\.(wav|mp4|webm)$`）で検証してから
  パスにします。パストラバーサルの余地がありません。
- 写真のパスは `/panels/名前.拡張子` の形だけを通します（**SSRF対策**）。
  取得先はサーバー起動時に決めた `FRONTEND_ORIGIN` に固定です。
- タイムラインには上限があります（区間数・総再生時間・1区間の長さ）。

### 5. 外部に送っていないこと

音声生成は **Colab 内で完結**します。子どもの音声を第三者のクラウドAPIに
送りません。これは「ローカルGPUで声クローン」を選んだことの直接の利点です。

### 6. 機械的な検査

- `schemathesis` が OpenAPI から生成した入力でエンドポイントを叩きます
- `Hypothesis` が任意の文字列で「保管ディレクトリの外を指さない」ことを確かめます
- `ruff` の bandit ルール（`S`）を有効にしています

---

## 何は守れていないか（正直に）

### トークンは秘密ではない

`VITE_EVENT_TOKEN` はフロントのバンドルに載ります。アプリのソースを
読める人には見えます。守れるのは「アプリのURLを踏んだだけの第三者」までで、
**本気の攻撃者は防げません**。

イベント当日限りの短命な値を使い、終わったらサーバーを止めてください。

### トンネルのURLは公開情報に近い

名簿（GAS）は無認証で読めるため、アプリのURLを知っていれば
バックエンドのURLも分かります。だからこそトークンが要ります。

### TTL 内の共有URLは誰でも開ける

QRコードで渡す動画は、TTL（既定30分）の間、URLを知っていれば
誰でもダウンロードできます。**本人・保護者の端末で読み取る運用**にしてください。

### 端末側には残る

作品と音声は端末の IndexedDB に残ります。
**次の子に渡す前に、必ず `/admin` の「作品をリセット」を実行してください。**

### レート制限がない

同じトークンを持つ人が何度でも生成を投げられます。
10人規模の閉じたイベント前提の割り切りです。

---

## 運用のチェックリスト

イベント前:

- [ ] `EVENT_TOKEN` を設定した（フロントとバックエンドで同じ文字列）
- [ ] `CORS_ORIGINS` を本番のオリジンに固定した（`*` にしない）
- [ ] 保護者・学校から録音とAI生成の同意を得た
- [ ] `bash scripts/smoke-test.sh <URL> <TOKEN>` が全部 PASS した

イベント後:

- [ ] `POST /cleanup` を各台に投げた
- [ ] Colab のランタイムを停止した（バックグラウンド実行は動き続けます）
- [ ] 使った端末で「作品をリセット」した

運用の詳細は [README](README.md#6-子どもの声の扱い)、設計の背景は [docs/adr](docs/adr) を参照してください。
